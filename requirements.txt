# CodeArena — Python backend dependencies
# Install with: pip install -r requirements.txt

# Web framework & ASGI server
fastapi==0.111.0
uvicorn[standard]==0.30.1

# WebSocket support
websockets==12.0

# HTTP client (used by grok.py and problems.py for API calls)
httpx==0.27.0

# Request body parsing (required by FastAPI for POST endpoints)
python-multipart==0.0.9

# Environment variable loading (.env support)
python-dotenv==1.0.1
